document.addEventListener('DOMContentLoaded', function() {
  const heroCarousel = document.querySelector('.hero-carousel');
  if (!heroCarousel) return;

  // For now, just one slide. In future, add multiple slides and rotation logic
  // Example: const slides = heroCarousel.querySelectorAll('.hero-slide');
  // Then add rotation with setInterval

  console.log('Hero carousel initialized');
});